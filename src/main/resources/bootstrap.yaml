zuul:
  routes:
    blog:
      path: /services/blog/**
      url: http://localhost:8081
      stripPrefix: true
      sensitiveHeaders:
      ignored-headers: Access-Control-Allow-Credentials, Access-Control-Allow-Origin
