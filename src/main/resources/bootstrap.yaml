zuul:
  routes:
    blog:
      path: /services/blog/**
      url: http://localhost:8081
      stripPrefix: true
      sensitiveHeaders:

spring:
  application:
    name: gateway
  profiles:
    active: default
  cloud:
    config:
      uri: http://localhost:8888

